name: split-v
description: "Two vertical panes (left/right)"

settings:
  bind_keys:
    - key: g
      action: "run-shell \"peakypanes popup\""

windows:
  - name: dev
    layout: even-horizontal
    panes:
      - title: left
        cmd: ""
      - title: right
        cmd: ""
